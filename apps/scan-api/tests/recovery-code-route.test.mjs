import assert from "node:assert/strict";
import test from "node:test";
import { buildApp } from "../src/app.mjs";
import { hashPassword,hashToken } from "../src/auth.mjs";
import { loadConfig } from "../src/config.mjs";

test("a recovery code is atomically consumed and never sent to storage in plaintext",async(t)=>{let matchedParams,sessionInserted=false;const userId="11111111-1111-4111-8111-111111111111",tenantId="22222222-2222-4222-8222-222222222222",db={query:async(sql,params=[])=>{if(sql.includes("WITH matched AS")){matchedParams=params;return{rowCount:1,rows:[{user_id:userId,tenant_id:tenantId}]};}if(sql.includes("DELETE FROM admin_sessions"))return{rowCount:0,rows:[]};if(sql.includes("SELECT ip_hash"))return{rowCount:0,rows:[]};if(sql.includes("INSERT INTO admin_sessions")){sessionInserted=true;assert.equal(params[3],tenantId);return{rowCount:1,rows:[]};}if(sql.includes("INSERT INTO authentication_events"))return{rowCount:1,rows:[]};throw new Error(`Unexpected SQL: ${sql}`);}};const config=loadConfig({NODE_ENV:"test",DATABASE_URL:"postgres://unused",AUTH_MODE:"local",ADMIN_PASSWORD_HASH:hashPassword("legacy-secret"),SESSION_COOKIE_SECURE:"false",LOG_LEVEL:"silent"});const app=await buildApp({config,db});t.after(()=>app.close());const code="RECOVERY-CODE-ABC123";const response=await app.inject({method:"POST",url:"/api/auth/recovery-codes/consume",payload:{username:"owner@example.com",code}});assert.equal(response.statusCode,200);assert.equal(response.json().recoveryCodeConsumed,true);assert.equal(matchedParams[0],hashToken(code));assert.equal(matchedParams.includes(code),false);assert.equal(sessionInserted,true);assert.ok(response.headers["set-cookie"]);});
